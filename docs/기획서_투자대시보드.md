# Stock Manager

**AI 기반 멀티팩터 투자 분석 대시보드 서비스 기획서**

---

## Executive Summary

Stock Manager는 **40개 정량 팩터와 규칙 기반 분석 엔진**을 결합하여, 투자 데이터의 수집·분석·시각화·인사이트 생성을 자동화하는 범용 금융 분석 대시보드입니다.

개인 투자자가 겪는 세 가지 구조적 문제 — **① 분석 기준의 주관성, ② 데이터 품질의 불균일성, ③ 해석 결과의 재현 불가능성** — 을 Skills.md라는 단일 규칙 문서 체계로 해결하며, 티커·테마·포트폴리오 등 다양한 입력을 동일한 분석 파이프라인에서 처리합니다.

본 기획서는 서비스의 가치 제안, 분석 아키텍처, 대시보드 구성, Skills.md 설계 방향, 그리고 확장 로드맵을 정의합니다.

---

## 1. 서비스 개요

### 1.1 서비스 정의

**Stock Manager**는 Skills.md에 정의된 일관된 분석 규칙을 기반으로, 투자 데이터를 자동으로 수집·분석·시각화하고 실행 가능한 투자 인사이트를 생성하는 AI 투자 분석 대시보드 서비스입니다.

### 1.2 문제 정의 (Problem Statement)

개인 투자자 및 실무 애널리스트는 다음과 같은 반복적 문제에 직면합니다.

| 영역 | 기존 문제 | Stock Manager 해결 방식 |
|------|-----------|------------------------|
| 데이터 수집 | 데이터 구조가 매번 달라 일관된 분석이 어려움 | 티커·테마·포트폴리오 입력을 단일 파이프라인으로 자동 처리하며, 8단계 품질 검증을 수행 |
| 분석 기준 | 분석 기준이 주관적이고 편향됨 | 40개 정량 팩터와 섹터별 동적 가중치를 통해 객관적 스코어링 체계 구축 |
| 시각화 | 차트 선택에 전문 지식이 필요 | 데이터 유형에 따라 차트 자동 매핑 규칙 적용 |
| 해석 | 지표 해석이 어렵고 추상적 | 6단계 투자 시그널과 자연어 인사이트, LLM 기반 리서치 리포트 생성 |
| 재현성 | 동일 데이터에 대한 분석 결과가 일관되지 않음 | Skills.md 규칙에 따라 누가 실행해도 동일한 결과 보장 |

### 1.3 핵심 차별점 (Differentiators)

**1. 규칙 기반 범용성 (Rule-based Universality)**
Skills.md 하나로 모든 분석 로직이 명시적으로 정의됩니다. 새로운 데이터가 입력되어도 동일한 규칙으로 분석이 수행됩니다.

**2. 멀티팩터 통합 스코어링 (Multi-factor Scoring)**
재무(20) + 기술적(10) + 감성(10) 팩터를 통합한 0~100 복합 스코어를 제공합니다. 단일 지표가 아닌 다차원 관점으로 종목을 평가합니다.

**3. 시장 적응형 가중치 (Adaptive Weighting)**
SPY 60일 수익률을 기반으로 강세장·약세장·중립 시장을 판정하고, 팩터 가중치에 보정 승수를 동적으로 적용합니다. 11개 섹터별 가중치 테이블도 별도로 관리됩니다.

**4. 실행 가능한 인사이트 (Actionable Insight)**
"Strong Buy ~ Strong Sell" 6단계 시그널, 포트폴리오 4단계 건강 진단, 딥 바잉(역발상 매수) 기회 탐지 등 즉시 실행 가능한 판단 근거를 제공합니다.

**5. LLM 기반 리서치 리포트 (AI Research Report)**
Google Gemini 3.1 Flash-Lite를 활용하여, 수집된 정량 데이터와 관련 뉴스를 종합한 한국어 투자 리서치 리포트를 각 페이지 하단에서 원클릭 생성합니다.

### 1.4 타겟 사용자 (Target Users)

| 사용자 유형 | 활용 시나리오 | 기대 효과 |
|------------|--------------|-----------|
| 개인 투자자 | 보유 종목 건강 진단, 신규 투자 후보 탐색 | 객관적 판단 근거 확보 |
| 투자 커뮤니티 | 테마별 종목 분석 공유, 랭킹 비교 | 토론 품질 제고 |
| 금융 애널리스트 | 멀티팩터 기반 리포트 자동 생성 | 리서치 시간 단축 |
| 핀테크 서비스 | API 연동을 통한 투자 분석 기능 탑재 | 서비스 차별화 |

### 1.5 비즈니스 가치 (Business Value)

- **시간 절감**: 40개 팩터 수집·계산 프로세스 자동화로 종목 분석 소요 시간을 90% 이상 단축
- **분석 품질 표준화**: Skills.md 규칙 기반으로 분석 결과의 일관성과 재현성 확보
- **확장성**: 섹터·시장·입력 형식 등 주요 축에 대한 범용 설계로 신규 도메인 추가 용이
- **AI 네이티브**: LLM 리포트 기능으로 정량 데이터와 정성적 해석을 하나의 사용자 경험으로 통합

---

## 2. 분석 흐름 설계

### 2.1 전체 분석 아키텍처

Stock Manager의 분석 엔진은 다음 4개 레이어로 구성됩니다.

**① 입력 레이어**
사용자는 세 가지 방식으로 분석을 시작합니다 — 티커 직접 입력(예: AAPL, MSFT), 테마 키워드 선택(예: AI, 반도체), 포트폴리오 입력(TICKER:수량@진입가 형식).

**② 데이터 수집 레이어**
- 시장 데이터: yfinance, Finnhub
- 뉴스 및 감성: Google News RSS, VADER 감성 분석기
- 공시 정보: SEC EDGAR (8-K, 10-Q, 10-K)

**③ 분석 엔진 레이어 (Skills.md 기반)**
- 데이터 품질 검증 (8개 자동 체크)
- 재무 분석 (20개 지표) + 기술적 분석 (10개 지표) + 감성 분석 (10개 지표)
- Z-Score 정규화 + 섹터별 동적 가중치 + 시장 상황 보정 승수
- 복합 스코어 (0~100) + 딥 보너스 가산

**④ 출력 레이어**
- 투자 시그널 6단계 판정 (Strong Buy ~ Strong Sell)
- 차트 자동 매핑 및 렌더링 (Recharts 기반)
- 자연어 인사이트 카드
- LLM 종합 분석 리포트 (Gemini 3.1 Flash-Lite)

프론트엔드는 Next.js 16(App Router) + Tailwind CSS v4로 구현되며, 백엔드 FastAPI 서버와 REST API로 통신합니다.

### 2.2 분석 파이프라인 상세

Stock Manager는 사용자 시나리오에 따라 네 가지 분석 파이프라인을 제공합니다.

#### Pipeline A — 테마 분석 (Theme Analysis)

AI가 추천하는 트렌딩 테마 또는 사용자가 직접 입력한 테마 키워드 하나로 관련 종목 발굴부터 랭킹까지 자동 수행합니다.

1. 테마 → 관련 ETF 매핑 (예: AI → BOTZ, AIQ)
2. ETF Top Holdings 추출로 후보 종목 리스트 생성
3. 시가총액 필터링 (AI 테마 기준 $10B 이상)
4. 3개 검색 쿼리로 뉴스 수집 (산업 동향 / 수요 전망 / 규제 이슈)
5. 종목별 SEC 공시 3건 수집 (8-K, 10-Q, 10-K)
6. 40개 팩터 수집 및 스코어링
7. 딥 보너스 계산 (낙폭 + 반등 확인)
8. 종합 랭킹 생성

**출력**: 테마 인사이트 카드 + 점수 바 차트 + 상세 랭킹 테이블 + LLM 종합 리포트

#### Pipeline B — 포트폴리오 진단 (Portfolio Diagnosis)

보유 종목, 수량, 진입가를 입력하면 실시간 P&L 추적과 건강 상태 진단을 수행합니다.

1. 종목별 현재가 조회 및 P&L 계산
2. 4단계 Phase 분류 (Uptrend / Stable / Unstable / Critical)
3. 포트폴리오 건강도 점수 산출 (0~100)
4. 리스크 알림 및 긍정 시그널 분류
5. 포트폴리오 구성 비율 및 종목별 P&L 시각화

**출력**: 요약 카드 4종 + 보유 종목 테이블 + 도넛 차트 + P&L 바 차트 + 알림 카드 + LLM 종합 리포트

#### Pipeline C — 종합 종목 분석 (Comprehensive Analysis)

단일 종목에 대한 딥다이브 분석을 수행합니다.

1. 재무 20개 지표 수집 및 분석
2. 기술적 10개 지표 계산 및 시그널 판정
3. 감성 10개 지표 분석 (뉴스 + 애널리스트 컨센서스 + 시장 심리)
4. 복합 스코어 산출 (섹터 가중치 + 시장 보정)
5. 투자 판단 근거 및 리스크 평가 생성
6. 팩터별 해석 (재무 / 기술적 / 감성 각각의 자연어 인사이트)

**출력**: 투자 시그널 카드 + 6대 팩터 레이더 차트 + 투자 근거 + 리스크 + 펀더멘털 요약 + 가격 차트 + LLM 종합 리포트

#### Pipeline D — 딥 바잉 레이더 (Dip Buying Radar)

낙폭 과대 + 반등 조짐 종목을 자동 탐지합니다.

1. 180일 고점 대비 낙폭 계산
2. 10일 반등 모멘텀 확인
3. 딥 보너스 산식 적용: `Dip Bonus = 0.5 × 낙폭점수 + 0.5 × (낙폭 × 모멘텀)`
4. 복합 점수와 합산하여 최종 스코어링

**출력**: 딥 바잉 후보 리스트 (낙폭률, 반등률, 시그널)

### 2.3 데이터 품질 보장

모든 분석에 앞서 8개 항목에 대한 자동 검증을 수행합니다.

**검증 항목**: ① 필수 컬럼(OHLC) 존재, ② 데이터 타입(숫자형), ③ 날짜 연속성(3일 초과 갭 없음), ④ 결측치 비율 1% 미만, ⑤ 이상치(3σ) 5건 미만, ⑥ 제로값 부재, ⑦ 가격 일관성(High ≥ Low), ⑧ 거래량 유효성.

**품질 점수 체계**

| 점수 | 등급 | 처리 방식 |
|------|------|-----------|
| 95% 이상 | Excellent | 정상 분석 |
| 85 ~ 95% | Good | 정상 분석 |
| 70 ~ 85% | Fair | 경고 표시 후 분석 |
| 50 ~ 70% | Poor | 자동 클리닝 후 분석 |
| 50% 미만 | Critical | 분석 불가 안내 |

---

## 3. 대시보드 구성

### 3.1 레이아웃 및 페이지 구조

서비스는 좌측 240px 사이드바와 우측 메인 콘텐츠 영역의 2단 구조를 채택합니다. 각 분석 페이지의 하단에는 LLM 종합 분석 리포트 섹션이 공통적으로 포함됩니다.

| 순번 | 페이지명 | 경로 | 역할 |
|------|---------|------|------|
| 1 | Market Overview | `/` | 시장 지수 및 섹터 성과 요약 (미국+한국 지수 6종) |
| 2 | Theme Explorer | `/theme` | AI 추천 테마별 종목 발굴 및 분석 (US/KR 탭) |
| 3 | Stock Analyzer | `/stock` | 개별 종목 40팩터 딥다이브 (기업명/코드 둘 다 지원) |
| 4 | Portfolio Dashboard | `/portfolio` | 보유 종목 건강 진단 및 P&L 추적 (US/KR 혼합 입력) |
| 5 | Ranking Engine | `/ranking` | 프리셋 그룹 종목 비교 및 랭킹 (US + KR 프리셋) |
| 6 | **AI Chatbot** | `/chat` | **자연어 투자 질의 — 13개 MCP 도구 기반 대화형 분석** |

> 별도의 리포트 센터 페이지 없이, 각 분석 페이지(2~5) 하단에 **LLM 종합 분석 리포트** 기능을 통합하여 데이터와 해석의 맥락이 분리되지 않도록 설계했습니다.

### 3.2 각 페이지 상세 구성

#### Page 1. Market Overview

시장의 전체적인 분위기를 한눈에 파악하는 진입 페이지로, 사용자 접속 시 자동으로 데이터가 로딩됩니다.

- **시장 상황 게이지**: Bull / Neutral / Bear 판정과 SPY 60일 수익률 표시
- **주요 지수 카드** (6종, `grid md:grid-cols-3 lg:grid-cols-6`): S&P 500, NASDAQ, DOW, VIX, **KOSPI (^KS11), KOSDAQ (^KQ11)** — 각각 현재가, 등락률, 30일 미니 AreaChart
- **섹터별 성과 바 차트**: 11개 섹터(Technology, Financial, Healthcare, Energy, Consumer, Industrials, Utilities, Real Estate, Materials, Communication, Staples)의 1개월 수익률
- **성능 보강**: `mapLimit(4)` 동시성 제한 + 지수 백오프 재시도 (`withRetry`) 로 15개 병렬 요청 시에도 Rate-limit 안전

#### Page 2. Theme Explorer

AI 추천 트렌딩 테마 또는 사용자 입력 테마로 관련 종목을 자동 발굴하고 분석합니다.

- **시장 탭**: `US 테마` / `한국 테마` — 별도 탭으로 서로 독립 상태 유지
- **US 테마**: ETF Top Holdings 기반 AI 추천 (AI, 반도체, 클라우드 등)
- **한국 테마**: 큐레이션된 18개 테마 (`docs/data/kr_themes.json`) — 2차전지, 원전, AI반도체, 조선, 바이오, 방산, 로봇, 전력설비, 자동차, 반도체장비, 자율주행, 게임, 엔터, 리츠, 화장품, 음식료, 건설, 반도체소재
- **입력 방식**: 트렌딩 테마 pill 버튼 + 직접 입력 텍스트 필드
- **테마 인사이트 카드**: 테마의 최상위 종목과 점수 기반 요약 문장
- **Top Stocks 점수 바 차트**: 종합 점수 기준 BarChart (1위 파란색, 2~3위 청록색, 나머지 회색 그라데이션)
- **종목 랭킹 테이블**: 순위, 종목명(한국은 한글+코드 병기), 종합 점수, 시그널 배지, 재무/기술/퀄리티 점수, 섹터
- **LLM 종합 분석 리포트**: 하단 '리포트 생성' 버튼

#### Page 3. Stock Analyzer

단일 종목에 대한 40팩터 심층 분석 대시보드입니다. **미국/한국 양 시장 동시 지원**.

- **MarketSelector** (`AUTO / US / KR` pill): 강제 분기 또는 티커 기반 자동 감지 (Hangul 도 KR 로 자동 분류)
- **입력 방식**: 티커 검색 바 — 아래 형태 모두 허용
  - `AAPL`, `NVDA` (US, 알파벳)
  - `005930` (KR-KOSPI, 6자리)
  - **`247540` (KR-KOSDAQ, 6자리)** — `kr_market_lookup` 이 자동으로 `.KQ` 매핑
  - `005930.KS`, `247540.KQ` (KR, 접미 형태)
  - **`"삼성전자"`, `"에코프로비엠"`, `"삼성"` (prefix)** (KR, 한글명 → 자동으로 6자리 코드 변환)
- **Resolution badge**: 검색 후 `✓ 삼성전자 (005930)` 입력창 옆 배지로 어떤 종목으로 매칭됐는지 즉시 표시
- **input → canonical 코드 sync**: API 응답 후 `setTicker(comp.ticker)` 로 canonical 코드 동기화
- **로케일 자동 전환**: KR = `₩214,500`, US = `$190.12` (`formatPrice(value, market)`)
- **한글명 헤더**: KR 종목은 "삼성전자 (005930)" dual-label
- **투자 시그널 카드**: 복합 점수(0~100), Buy/Hold/Sell 배지, 투자 판단 이유
- **6대 팩터 레이더 차트**: Financial, Technical, Sentiment, Growth, Quality, Value
- **투자 판단 근거 카드**: Decision + Confidence + 매수 근거 리스트
- **리스크 평가 카드**: 위험 요소 리스트 또는 "특별한 리스크 없음"
- **팩터별 해석 카드 (3종)**: 재무 / 기술적 / 감성 분석 각각의 자연어 인사이트
- **핵심 펀더멘털 요약**: Market Cap, P/E, EPS, P/B, ROE, Profit Margin, Revenue Growth, Sector — 통화 locale 자동 적용, `tabular-nums`
- **6개월 가격 차트**: 그라데이션 AreaChart
- **LLM 종합 분석 리포트**: 하단 '리포트 생성' 버튼

#### Page 4. Portfolio Dashboard

보유 종목의 건강 상태, P&L, 리스크 알림을 종합 모니터링합니다.

- **입력 방식**: `TICKER:수량@진입가` 형식 — **한글명 토큰도 허용** (예: `AAPL:50@150, 삼성전자:10@70000, 247540:20@90000, MSFT:30@350`). 한글명은 backend resolver 가 자동으로 6자리 코드로 변환. **US/KR 혼합 가능**. 기준 통화는 **USD 고정** (KR 종목 진입가는 USD 환산 입력).
- **Holdings 테이블 dual-label**: KR 종목은 `삼성전자 (005930)` 형식, US 는 코드만 (`AAPL`)
- **요약 카드 (4종)**: 총 자산, 총 손익, 건강도 점수, Phase
- **보유 종목 분석 테이블**: 종목, 수량, 진입가, 현재가, P&L 금액, P&L %, 점수, 시그널 배지
- **포트폴리오 구성 도넛 차트**: 종목별 비중 + 현금 비중
- **종목별 P&L 수평 바 차트**: 이익(녹색) / 손실(빨간색)
- **리스크 알림 카드**: 변동성, 데드 크로스 등 경고
- **긍정 시그널 카드**: 상승 추세, 수익성 양호 등
- **LLM 종합 분석 리포트**: 하단 '리포트 생성' 버튼

#### Page 5. Ranking Engine

프리셋 그룹 또는 직접 입력으로 후보 종목을 비교 랭킹합니다.

- **입력 방식**: 쉼표 구분 티커 직접 입력 (digit-only 6자리는 대문자 변환 스킵) + 프리셋 버튼
  - **US 프리셋**: Mega Cap Tech, Semiconductor, Healthcare, Finance
  - **KR 프리셋** (신규): **KR 반도체 / KR 2차전지 / KR 바이오 / KR 방산**
- **혼합 basket 지원**: `AAPL, 삼성전자, NVDA, 247540` — 한글명 + KOSPI + KOSDAQ + US 모두 한 입력에 가능, backend resolver 가 정규화
- **Dual-label 테이블**: KR 종목 행은 `삼성전자 (005930)` 표시, US 는 `AAPL`
- **단일 시장 basket 최적화**: 입력이 전부 KR 이면 `_run_factor_ranking` 이 `market="KR"` 로 FactorAggregator 호출 (yfinance 404 회피, DART 경로 활용)
- **랭킹 인사이트 카드**: 최우선 추천 종목, 평균 점수, 매수/매도 시그널 통계
- **종합 점수 바 차트**: 점수별 색상 차등 (70+ 청록, 50~70 파랑, 30~50 주황, 30 미만 빨강)
- **상세 랭킹 테이블**: 순위(메달 배지), 종목명, 종합 점수, 시그널, 재무/기술/성장/퀄리티 점수, 팩터 수, 섹터
- **LLM 종합 분석 리포트**: 하단 '리포트 생성' 버튼

#### Page 6. AI Chatbot (신규)

MCP 분석 도구를 자연어로 질의하는 대화형 workbench. **한국어·영어 동시 지원**.

- **2-pane 레이아웃** (md+): 대화 영역 + Artifact Panel
- **ChatHeader**: Model Selector (**gemini-3.0-flash 기본**, 2.5-flash / 2.0-flash / 1.5-flash / 1.5-pro 선택), ⌘K 팔레트, New Chat, 다크모드 토글, 세션 뱃지
- **13개 도구**: market/stock/ranking/theme/news + discovery (propose_tickers, dip_candidates, watchlist_signals) + KR 전용 (propose_themes_kr, analyze_theme_kr, dart_filings)
- **SSE 스트리밍** (`GET /api/chat/stream`):
  - `tool_call` → "실행 중…" 칩 즉시 표시
  - `tool_result` → summary + 소요 ms 로 업데이트
  - `token` → BlinkingCaret 타자기 효과
  - `done` / `error`
- **통화 자동 표기**: 도구 결과의 `market`/`currency` 필드에 따라 LLM이 ₩/$ 기호 자동 사용
- **중단 버튼**: AbortController 로 서버 루프 5s 내 종료
- **Fallback**: 첫 SSE 이벤트 전 실패 시 POST `/api/chat` 자동 재시도
- **빠른 시작 칩 6개**: AI 반도체(US), AAPL 매수 의견, **한국 2차전지 추천**, **삼성전자(005930) 분석**, **삼성 최근 공시**, 시장 국면
- **세션 메모리**: 30분 TTL, 20턴 rolling window
- **Rate limit**: 20/min per IP

#### 공통 — LLM 종합 분석 리포트

각 분석 페이지 하단에 통합된 AI 리포트 생성 기능입니다. '리포트 생성' 버튼 클릭 시 수집된 데이터를 기반으로 LLM이 한국어 투자 리서치 리포트를 생성합니다.

| 항목 | 사양 |
|------|------|
| LLM 엔진 | Google AI Studio — Gemini 3.1 Flash-Lite Preview |
| 출력 언어 | 한국어 (전문 용어 한국어+영문 병기) |
| 타임아웃 | 300초 |
| 뉴스 소스 | Google News RSS (URL + snippet 포함, HTML 자동 제거) |

**리포트 구성**

1. **AI 종합 분석 요약**: 종합 요약 → 핵심 근거 → 리스크 및 우려사항 → 결론 (300~500단어 한국어 리서치 리포트, 뉴스 인용 포함)
2. **관련 뉴스 & 이벤트**: 제목 클릭 시 원문 링크 이동, snippet 미리보기, 출처 및 날짜 표시
3. **핵심 근거 데이터**: 종합점수, 시그널, 섹터, 재무/기술 점수, P/E, ROE, 매출성장률, 뉴스 센티먼트 등 정량 지표 요약

### 3.2.7 Rich Visual Reports (v3.0 신규) — ShowMe 스타일 구조화 응답

분석 리포트(`/api/*/analysis-report`) + 챗봇 도구 결과(`tool_result.artifact`)
모두 **단일 long markdown** 대신 **블록 배열 JSON** 으로 반환된다. 프론트의
`BlockRenderer` 가 dispatcher 로 11종 위젯에 분배 렌더.

| 블록 종류 | 용도 |
|---|---|
| `summary` | 제목 + Markdown 본문 + `[1][2]` 인용 마커 |
| `metric` / `metric_grid` | BigNumber 카드 그리드 (delta, tone, hint) |
| `factor_bullet` | 6대 팩터 점수 막대 (재무/기술/성장/퀄리티/밸류/모멘텀) |
| `news_citation` | 번호 bubble 5건 + 외부 링크 + snippet |
| `price_spark` | 6개월 미니 area chart + delta % |
| **`candlestick`** | 거래량 sub-pane + MA20/MA50 overlay (커스텀 SVG, 의존성 0) |
| `table` | sortable headless table, `tabular-nums`, 4 format 타입 |
| `heatmap` | CSS-grid 상관관계/heat (RdBu 팔레트) |
| `sector_treemap` | flex squarified treemap (PnL tint) |
| `radar_mini` | 6 팩터 RadarChart |

**핵심 설계**:
- **Deterministic-first**: 수치는 Python 빌더가 생성, LLM 은 `summary` + `factor_bullet` 만 작성 → 재현성 + 신뢰성
- **3-strategy JSON 파서**: 전체 JSON → balanced bracket → prose fallback (LLM 이 schema 안 지켜도 항상 답변 보장)
- **다크 모드 전용 팔레트** (CSS 변수 9종 — `--chart-grid/axis/tooltip-bg/pos/neg/...`) → `<html data-theme>` swap 시 즉시 반영
- **framer-motion stagger** + `useReducedMotion` (a11y 자동)
- **챗봇 통합**: rank_stocks/analyze_theme 등 도구 결과 → `[TableBlock, RadarMiniBlock]` 자동 변환 → 채팅 메시지 하단에 인라인 렌더

### 3.3 시각화 자동 매핑 체계

Skills.md에 정의된 규칙에 따라 데이터 유형별로 차트가 자동 선택됩니다.

| 데이터 유형 | 차트 | 적용 페이지 |
|------------|------|------------|
| 주요 지수 추이 | Mini AreaChart | Market Overview |
| 섹터별 성과 | Horizontal BarChart | Market Overview |
| 팩터 스코어 | RadarChart (6축) | Stock Analyzer |
| 가격 추이 | Gradient AreaChart | Stock Analyzer |
| 종목 랭킹 점수 | Vertical BarChart | Theme Explorer, Ranking Engine |
| 포트폴리오 구성 | Donut PieChart | Portfolio Dashboard |
| 종목별 P&L | Horizontal BarChart | Portfolio Dashboard |

---

## 4. Skills.md 설계 방향

### 4.1 설계 철학

Skills.md는 분석 규칙의 **"단일 진실 원천(Single Source of Truth)"**으로 설계되었습니다. 모든 분석 로직은 이 문서에 숫자와 수식으로 명시되며, 구현은 문서의 규칙을 코드로 변환하는 방식을 취합니다.

**핵심 원칙 4가지**

1. **규칙 명시성**: 모든 임계값, 가중치, 수식을 숫자로 명시합니다. 예: "RSI > 70 → 과매수"는 허용하되, "RSI가 높으면 과매수"는 배제합니다.
2. **재현 가능성**: 동일한 데이터와 동일한 Skills.md 규칙이 적용되면 항상 동일한 결과가 산출되어야 합니다.
3. **확장 가능성**: 새로운 팩터나 섹터를 추가할 때 기존 구조를 변경하지 않고 규칙만 추가하면 되도록 설계합니다.
4. **범용성**: 특정 종목이나 시장에 종속되지 않는 일반화된 규칙을 지향합니다. 미국·한국 등 다중 시장 대응이 가능해야 합니다.

### 4.2 Skills.md 구성 체계

Skills.md는 다음 8개 섹션으로 구성됩니다.

| 섹션 | 내용 |
|------|------|
| 1. 데이터 분석 기준 정의 | 6대 팩터 체계, 11개 섹터별 동적 가중치, 3단계 시장 상황 보정 규칙, Z-Score 정규화 4단계 |
| 2. 투자 지표 계산 규칙 | 재무 팩터 20개, 기술적 지표 10개, 감성 팩터 10개, 복합 스코어, 딥 보너스, 포트폴리오 4단계 진단, 모멘텀, 백테스트 검증 |
| 3. 시각화 선택 기준 | 차트 자동 매핑, 색상 팔레트, 반응형 레이아웃 |
| 4. 리포트 구성 흐름 | 테마 분석 파이프라인(8 Step), 포트폴리오 진단, 종합 분석, 딥 후보 탐색 |
| 5. 인사이트 생성 규칙 | 9점 만점 종합 판정, 감성 모멘텀 판정(5단계), 감성 신뢰도, 트렌딩 감지, 자연어 템플릿(6종), 위험 경고 조건 |
| 6. 데이터 품질 관리 | 8개 검증 체크, 5단계 품질 점수, 3종 자동 클리닝 |
| 7. 범용성 설계 | 다중 시장 지원(US/KR), 3가지 입력 형식(티커/테마/CSV), 6단계 TTL 캐시 정책 |
| 8. 대시보드 페이지 구성 | 5개 페이지 구성 요소 + LLM 종합 리포트 |

### 4.3 바이브코딩 활용 전략

Skills.md는 단순 문서가 아니라 AI 개발 에이전트(Claude)에게 전달하는 **실행 가능한 스펙**입니다.

- "Skills.md 섹션 2.2의 RSI 계산 규칙을 기반으로 `technical_indicators.py`를 구현"
- "섹션 3.1의 차트 매핑 규칙대로 데이터 유형별 차트 컴포넌트를 자동 선택하는 `ChartSelector` 컴포넌트 생성"
- "섹션 5.5의 자연어 템플릿을 사용하여 투자 시그널을 인사이트 카드로 렌더링"
- "섹션 4.1 테마 분석 파이프라인의 8단계 흐름을 API로 구현"

이러한 접근은 **문서가 곧 스펙이고, 스펙이 곧 코드로 변환**되는 개발 흐름을 만들어, 분석 로직의 변경이 항상 Skills.md에서 시작되도록 보장합니다.

---

## 5. 확장 기능 로드맵

### 5.1 단기 로드맵 (v2.0 기준 — 2026-04-20)

| 기능 | 설명 | 상태 |
|------|------|------|
| AI 종합 리포트 | Gemini 기반 한국어 투자 리서치 리포트 자동 생성 | ✅ 구현 완료 (v1.0) |
| **AI Chatbot** | MCP 도구 13개 기반 대화형 자연어 투자 질의 | ✅ **구현 완료 + archived** (4 사이클: core/streaming/performance/ux) |
| **다크 모드** | `<html data-theme>` + localStorage 기반, 전 페이지 적용 | ✅ **구현 완료** (mcp-chatbot-ux) |
| **한국 시장 기본 지원** | 티커 감지, PyKrx OHLCV, KR 테마, 한국어 뉴스 | ✅ **구현 완료** (kr-stock-integration P1) |
| **DART 연동** | 공시 목록 + K-IFRS 재무 ROE/ROA | ✅ **구현 완료** (kr-stock-integration P2) |
| **한국 기업명 입력** | "삼성전자" → 005930 자동 resolve | ✅ **구현 완료** |
| **KOSDAQ 자동 분류** | `247540` 등 KOSDAQ 종목 정상 분석 (kr_market_lookup) | ✅ **v3.0 구현 완료** |
| **한글명 ↔ 코드 양방향 + dual-label** | 모든 페이지에서 `삼성전자 (005930)` 통일 표기 | ✅ **v3.0 구현 완료** |
| **Rich Visual Reports** | ShowMe 스타일 11종 ReportBlock + 캔들스틱 + 다크 팔레트 | ✅ **v3.0 archived (92%)** |
| **Gemini 3.0 Flash 기본 모델** | 챗봇/리포트 모두 최신 Flash + 5단계 fallback chain | ✅ **v3.0 구현 완료** |
| 알림 시스템 | Critical 종목 발생 시 브라우저 알림 | 🟡 계획 |
| 비교 모드 | 2개 종목 Split View | 🟡 계획 |
| chart-polish | 차트 mode 탭, dashed grid, 반응형, skeleton | 🟡 다음 사이클 |
| a11y-audit | axe-core 자동화 | 🟡 다음 사이클 |

### 5.2 중기 로드맵

| 기능 | 설명 | 비즈니스 가치 |
|------|------|--------------|
| **한국 시장 실시간 (P3)** | KIS Developers OAuth2 실시간 호가/체결 | 거래 UX 강화 |
| **멀티 통화 포트폴리오** | USD/KRW 혼합 보유, 환율 API (`exchangerate.host`) 연동 | 국제 투자자 대응 |
| 한국 거래시간/휴장일 | KST 09:00~15:30, 캘린더 기반 휴장 안내 | 운영 신뢰성 |
| 외국인/기관 수급 | PyKrx 수급 데이터 시각화 | KR 고유 분석 지표 |
| mcp-chatbot v2 통합 | streaming + performance + ux 통합 릴리스 브랜치 | 챗봇 안정화 |
| 커뮤니티 랭킹 | 사용자 포트폴리오 성과 공유 | 네트워크 효과 |
| ETF 스크리너 | ETF 구성 종목 기반 섹터·테마 자동 분류 | 분석 편의성 |
| 자동 스케줄러 | 일별·주별 자동 분석 및 리포트 발송 | 업무 자동화 |

### 5.3 장기 비전

- **Phase 1** ✅: Skills.md 기반 대시보드 — 40팩터 + 5페이지 + LLM 리포트 (v1.0 목표, 달성)
- **Phase 2** ✅ 부분 달성 (v2.0): AI 챗봇 + MCP 도구 13개 + SSE 스트리밍 + 한국 시장 기본 지원
- **Phase 3**: 멀티 에이전트 자율 분석 — 시장 모니터링 에이전트가 이상 감지 → 포트폴리오 리밸런싱 제안 → 알림·분석·리포트 전자동화
- **Phase 4**: 글로벌 확장 — 미국·한국·일본·유럽 시장 통합 분석, 다국어 인사이트, 환율/매크로 팩터 통합

---

## 6. 기술 스택

| 영역 | 기술 | 선택 이유 |
|------|------|-----------|
| 프론트엔드 | Next.js 16 (App Router, Turbopack) | SSR/SSG 지원, 빠른 초기 로딩 |
| UI 프레임워크 | Tailwind CSS v4 (@theme tokens) | 일관된 디자인 토큰, 다크 모드 `data-theme` 스왑 |
| 차트 라이브러리 | Recharts (+ LazyChart render-prop) + 커스텀 SVG CandleShape | 반응형 차트 + 코드 스플리팅 + 캔들스틱 (의존성 0) |
| **블록 렌더러** | `BlockRenderer` dispatcher + 11종 블록 컴포넌트 | ShowMe 스타일 구조화 응답 |
| **애니메이션** | framer-motion + `useReducedMotion` | 차트/리포트 mount fade/stagger, a11y 자동 |
| Markdown | react-markdown + remark-gfm | LLM 리포트 렌더링, 손수 구현 파서 제거 |
| 상태 관리 | React useState + Context (ChatModelProvider) | 경량 구현, 보일러플레이트 최소 |
| SSE 클라이언트 | fetch + ReadableStream + AbortController | EventSource 대비 cancel·헤더 확장성 |
| 분석 엔진 | Python FastAPI (도메인별 라우터 split) | 40팩터 계산, Envelope[T] 응답 계약 |
| **US 데이터 소스** | yfinance, Finnhub, Alpha Vantage, Google News RSS | 가격·재무·뉴스·기술지표 |
| **KR 데이터 소스** | **PyKrx (OHLCV)**, **DART OPEN API (재무/공시)**, **Google News KR RSS**, `kr_market_lookup` 3-tier KOSPI/KOSDAQ classifier | 한국 상장사 전담 경로, AV/Finnhub 우회 |
| LLM 엔진 | **Google AI Studio (Gemini 3.0 Flash 기본, v3.0 신규)** | 한국어 리포트, 챗봇 tool-calling; preview 모델은 opt-in |
| **LLM 신뢰성** | `call_llm_resilient` — 5xx/429/404/circuit-open 분별 fallback chain (3.0 → 2.5 → 2.0 → 2.0-lite → 2.5-lite → 1.5-latest) | 운영 중 503/quota 자동 복구 |
| **LLM 출력 한도** | `LLM_MAX_OUTPUT_TOKENS=8192` | 한국어 리포트 잘림 방지 |
| 캐시 | DiskCache + SQLite (6단계 TTL) + `@lru_cache` + `@cached` | 외부 API 호출 50% 이상 절감, 일일/4시간/분 단위 |
| **관측성** | `/api/chat/metrics` (p50/p95 latency, error rate), 구조화 로그 | 경량 Prometheus 전 단계 |
| **Rate limit** | slowapi (default 240/min, chat 20/min, analysis 10/min) | 외부 API 보호 |
| 배포 | Vercel (프론트) + Python Server (백엔드) | 빠른 배포, 운영 안정성 |
| 의존성 (신규) | `pykrx`, `opendartreader`, `finance-datareader`(선택), `framer-motion` | 한국 시장 통합 + 블록 렌더러 애니메이션 |

---

## 7. API 엔드포인트 구성

### 7.1 REST API 개요

백엔드는 FastAPI로 구현되며, 다음과 같은 도메인별 엔드포인트를 제공합니다.

| 카테고리 | 엔드포인트 | 설명 |
|---------|-----------|------|
| 시장 (공통) | `GET /api/market/condition` | 미국 시장 상황 (Bull / Bear / Neutral, SPY 60D) |
| 시장 (공통) | `GET /api/market/prices` | 과거 가격 데이터 (KR/US 자동 분기) |
| **시장 (KR)** | **`GET /api/market/kr/condition`** | **KOSPI 60D 수익률 + 국면 판정** |
| **시장 (KR)** | **`GET /api/market/kr/indices`** | **KOSPI/KOSDAQ/KOSPI200 스냅샷** |
| **시장 (KR)** | **`GET /api/market/kr/prices`** | **KR 티커 OHLCV (PyKrx)** |
| 종목 분석 | `GET /api/stock/comprehensive` | 40팩터 종합 분석 (market/currency/name_kr 포함) |
| 종목 분석 | `GET /api/stock/signal` | 매수 / 보유 / 매도 시그널 |
| 종목 분석 | `GET /api/stock/investment-signal` | 투자 판단 근거 + 리스크 |
| 종목 분석 | `GET /api/stock/factor-interpretation` | 팩터별 해석 (재무 / 기술 / 감성) — market-aware |
| 종목 분석 | `GET /api/stock/analysis-report` | LLM 종합 분석 리포트 |
| 기술 분석 | `GET /api/technical/analyze` | 기술적 지표 분석 |
| 뉴스·감성 | `GET /api/news/search` | 뉴스 검색 (Google News RSS) |
| 뉴스·감성 | `GET /api/news/sentiment` | 뉴스 감성 분석 (KR 티커는 한국어 쿼리 자동) |
| 뉴스·감성 | `GET /api/news/timeline` | 뉴스 타임라인 |
| 포트폴리오 | `GET /api/portfolio/comprehensive` | P&L · 건강도 · 알림 종합 분석 |
| 포트폴리오 | `GET /api/portfolio/analysis-report` | 포트폴리오 LLM 리포트 |
| 랭킹 | `GET /api/ranking/stocks` | 다중 종목 랭킹 (단일 시장 basket → market pin) |
| 랭킹 | `GET /api/ranking/analysis-report` | 랭킹 LLM 리포트 |
| 테마 (US) | `GET /api/theme/propose` | AI 트렌딩 테마 추천 |
| 테마 (US) | `GET /api/theme/explore` | 테마 탐색 |
| 테마 (US) | `GET /api/theme/tickers` | 테마 관련 종목 추출 |
| 테마 (US) | `GET /api/theme/analyze` | 테마 종목 분석 |
| 테마 (US) | `GET /api/theme/analysis-report` | 테마 LLM 리포트 |
| **테마 (KR)** | **`GET /api/theme/kr/propose`** | **큐레이션된 한국 테마 목록 (18개)** |
| **테마 (KR)** | **`GET /api/theme/kr/tickers`** | **한국 테마 소속 종목 (코드 + 한글명)** |
| **테마 (KR)** | **`GET /api/theme/kr/analyze`** | **한국 테마 멀티팩터 랭킹** |
| **DART (KR)** | **`GET /api/dart/filings`** | **최근 공시 목록 (종목별 30일)** |
| **DART (KR)** | **`GET /api/dart/financials`** | **K-IFRS 재무 (ROE/ROA/Operating/Net/Revenue_Growth)** |
| **챗봇** | **`POST /api/chat`** | **Tool-augmented 대화 (세션 메모리)** |
| **챗봇 (SSE)** | **`GET /api/chat/stream`** | **스트리밍 — tool_call/tool_result/token/done/error 이벤트** |
| **챗봇** | **`GET /api/chat/session/{id}`** | **세션 transcript 조회 (디버그)** |
| **챗봇** | **`GET /api/chat/metrics`** | **p50/p95 latency, tool_error_rate, llm_errors** |
| 외부 | `GET /api/finnhub/summary` | Finnhub 종합 데이터 (US 전용) |
| 시스템 | `GET /api/health` | 헬스 체크 |
| 시스템 | `GET /api/circuit/status` | 서킷 브레이커 상태 |

### 7.2 LLM 리포트 응답 구조

4개 analysis-report 엔드포인트는 다음과 같이 표준화된 JSON 스키마를 반환합니다.

```json
{
  "summary": "## 종합 요약\n...\n## 핵심 근거\n...\n## 리스크\n...\n## 결론\n...",
  "news": [
    {
      "title": "기사 제목 (HTML 제거)",
      "source": "출처",
      "date": "2026-04-13",
      "url": "https://...",
      "snippet": "기사 미리보기 텍스트"
    }
  ],
  "evidence": {
    "종합점수": "82.3/100",
    "시그널": "Strong Buy",
    "섹터": "Technology",
    "재무점수": "91",
    "기술점수": "85"
  }
}
```

---

## 8. 결론

Stock Manager는 **Skills.md에 정의된 40개 팩터 분석 규칙**을 기반으로, 투자 데이터를 자동으로 수집·분석·시각화하고 실행 가능한 인사이트를 생성하는 범용 금융 투자 대시보드입니다.

**핵심 경쟁력 (v3.0)**

1. **Skills.md — 분석의 단일 진실 원천**: 40개 팩터, 차트 자동 매핑, 6단계 시그널이 모두 규칙으로 정의되어 재현 가능성과 확장성을 동시에 확보합니다.
2. **바이브코딩 — 문서가 곧 코드**: Skills.md 규칙을 AI가 코드로 변환하는 구조로, 로직 변경이 항상 문서에서 시작됩니다.
3. **한국 시장 1급 지원 (v2.0~v3.0)**: PyKrx OHLCV + DART K-IFRS 재무 + 한국어 뉴스 + 큐레이션 테마 18개. 한글 기업명 ↔ 6자리 코드 양방향 + KOSDAQ 자동 분류 + 모든 페이지 dual-label 표기.
4. **AI 챗봇 — 대화형 분석 (v2.0)**: 13개 MCP 도구 기반 tool-augmented generation. SSE 스트리밍으로 실시간 도구 실행 상태 표시, ⌘K 팔레트, 다크모드, 중단/재시도 UX.
5. **ShowMe-style Rich Visual Reports (v3.0 신규)**: LLM 응답을 11종 ReportBlock 으로 구조화 — 캔들스틱(거래량+MA), 히트맵, 트리맵, 라다, 메트릭 카드, 인용 등 카드/차트로 분해. Deterministic-first + 3-strategy 파서로 LLM 변동성에도 항상 답변 보장.
6. **운영 가능한 AI 인프라 (v2.0~v3.0)**: `call_llm_resilient` 로 429/503/404/circuit-open 분별 fallback (Gemini 3.0 → 2.5 → 2.0 → 2.0-lite → 2.5-lite → 1.5-latest 5단계 chain). `/api/chat/metrics` 로 p50/p95 관측, 구조화 로그, rate-limit + circuit breaker.
7. **실행 가능한 인사이트 — 판단이 아닌 행동 지침**: "Strong Buy" 시그널과 자연어 설명, 위험 경고를 통해 사용자가 즉시 행동할 수 있는 판단 근거를 제공합니다.
8. **LLM 종합 리포트 — AI 기반 리서치 자동화 (v3.0 model bump)**: Gemini 3.0 Flash 기반 한국어 리포트 (8192 토큰 한도), 뉴스 기사 인용 분석, 각 페이지 하단 원클릭 생성.

Stock Manager는 단순한 분석 도구가 아닌, **규칙 기반 투자 분석의 표준 플랫폼**을 지향하며,
v3.0 에서 **ShowMe 스타일 시각 리포트 + 한국 시장 KOSDAQ 풀 커버리지 + 양방향 매칭 + 최신 LLM** 으로 한 단계 더 확장했습니다.

---

*본 기획서는 v3.0 (2026-04-23) 기준입니다. 개발 진행은 `docs/archive/2026-04/` PDCA 아카이브에서 추적 가능합니다 (총 7 features archived: backend-quality-upgrade, frontend-quality-upgrade, mcp-chatbot, mcp-chatbot-streaming, mcp-chatbot-performance, mcp-chatbot-ux, rich-visual-reports).*
